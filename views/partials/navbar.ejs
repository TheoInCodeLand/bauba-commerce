<nav class="navbar">
  <div class="container nav-container">
    <a href="/products" class="logo">ShopMVP</a>

    <div class="nav-links">
      <a href="/products">Products</a>

      <% if (user) { %>
      <a href="/wishlist" class="wishlist-link" title="My Wishlist">
        ♥
        <% if (wishlistCount > 0) { %>
        <span class="badge wishlist-badge" id="wishlist-count"><%= wishlistCount %></span>
        <% } %>
      </a>
      <a href="/orders">My Orders</a>
      <a href="/cart" class="cart-link">
        Cart
        <% if (cartCount > 0) { %>
        <span class="badge" id="cart-count-badge"><%= cartCount %></span>
        <% } %>
      </a>

      <% if (user.is_admin) { %>
      <a href="/admin" class="admin-link">Admin</a>
      <% } %>

      <form action="/logout" method="POST" class="inline-form">
        <button type="submit" class="btn-link">Logout (<%= user.email %>)</button>
      </form>
      <% } else { %>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
      <a href="/cart" class="cart-link">
        Cart
        <% if (cartCount > 0) { %>
        <span class="badge" id="cart-count-badge"><%= cartCount %></span>
        <% } %>
      </a>
      <% } %>
    </div>
  </div>
</nav>